# 微网储能调度优化建模与求解报告

## 摘要

本文针对含光伏和电化学储能的微网购电调度问题，基于附件数据建立统一的线性规划模型，并分别研究确定型日内优化、日前计划与实时修正、滚动模型预测控制（MPC）以及全年滚动调度。

模型以购电成本最小为目标，决策变量包括电网购电量、储能充电量、储能放电量和荷电状态（SOC）。在给定的储能参数和供电约束下，使用 SciPy `linprog` 的 HiGHS 求解器完成优化。结果表明：在本工程设定下，频繁更新光伏预测的全年 MPC 策略相较固定电价日前计划方案，总成本降低 348,729.04 元，降幅 2.36%；紧急购电成本占比由 16.41% 降至 7.31%。

---

## 1 研究问题与数据

### 1.1 数据文件

工程使用根目录 `data/` 下的四个附件：

- `附件1.xlsx`：单日数据，字段为 `时间`、`电价`、`小区负载`、`光伏发电预测功率`；
- `附件2.xlsx`：全年数据，工作表为 `小区负载` 和 `光伏发电实际功率`；
- `附件3.xlsx`：光伏预测数据；
- `附件4.xlsx`：全年分时波动电价数据。

时间分辨率为 10 分钟，每天 144 个时段。附件2覆盖 2025 年全年 365 天。问题2和问题4按 2025 年 2 月 1 日至 12 月 31 日进行统计，共 334 天。

### 1.2 储能参数

工程中统一使用以下储能参数：

| 参数 | 数值 |
|---|---:|
| 额定容量 | 12,000 kWh |
| 最大充放电功率 | 5,000 kW |
| 单时段最大充放电量 | 833.33 kWh |
| 充放电效率 | 0.9 |
| SOC 初值 | 6,000 kWh |
| SOC 下限 | 1,200 kWh |
| SOC 上限 | 10,800 kWh |
| 调度步长 | 10 分钟 |

其中，单时段能量上限由
\[
C_{\max}=P_{\max}\Delta t
=5000\times\frac{1}{6}
=833.33\ {\rm kWh}
\]
得到。

---

## 2 线性规划模型

### 2.1 决策变量

对每个时段 \(t=1,\ldots,N\)，定义：

- \(g_t\)：电网购电量，单位为 kWh；
- \(c_t\)：储能充电量，单位为 kWh；
- \(d_t\)：储能放电量，单位为 kWh；
- \(s_t\)：时段结束时的储能 SOC，单位为 kWh。

### 2.2 供电平衡约束

设负载功率为 \(L_t\)，光伏功率为 \(P_t^{PV}\)，则对应时段的负载能量和光伏能量分别为 \(L_t\Delta t\) 与 \(P_t^{PV}\Delta t\)。供电约束为
\[
g_t+P_t^{PV}\Delta t+d_t-c_t
\ge L_t\Delta t.
\]

该约束保证负载需求得到满足。若实时光伏低于预测值，计划购电与计划储能动作可能无法完全覆盖负荷，差额记为紧急购电。

### 2.3 SOC递推约束

储能状态满足
\[
s_t=s_{t-1}+\eta c_t-\frac{d_t}{\eta},
\]
其中 \(\eta=0.9\) 为充放电效率，初始状态为
\[
s_0=6000.
\]

SOC边界为
\[
1200\le s_t\le10800.
\]

每时段充放电量满足
\[
0\le c_t\le833.33,\qquad
0\le d_t\le833.33.
\]

为保证日间调度不透支下一日运行状态，设置
\[
s_N=6000.
\]

### 2.4 目标函数

在电价为 \(p_t\) 时，购电成本最小化目标为
\[
\min \sum_{t=1}^{N}p_tg_t.
\]

该模型为线性规划，可直接由 HiGHS 求解。

---

## 3 两阶段实时结算模型

问题2和问题4采用计划—执行—结算结构。

第一阶段根据预测光伏和负载生成购电、充电和放电计划。第二阶段使用真实光伏数据进行结算。按照计划执行时，实际供给为
\[
q_t=g_t+P_{t,\mathrm{real}}^{PV}\Delta t+d_t-c_t.
\]

若实际供给不足，紧急购电量为
\[
e_t=\max\left(0,L_t\Delta t-q_t\right).
\]

紧急购电价格设为对应时段购电价格的 5 倍，因此总成本为
\[
C=\sum_t p_tg_t+\sum_t5p_te_t.
\]

---

## 4 问题1：确定型单日优化

问题1使用附件1中的单日固定电价、负载和光伏预测数据，并将预测视为准确数据进行全天优化。

求解结果：

| 指标 | 结果 |
|---|---:|
| 全天购电成本 | 35,126.95 元 |
| 初始 SOC | 6,000.00 kWh |
| 末态 SOC | 6,000.00 kWh |
| 最大供电缺口 | \(1.14\times10^{-13}\) kWh |

供电缺口达到机器精度量级，说明线性规划约束满足良好。结果文件为：

- `results/result1.xlsx`
- `results/problem1.json`

主图为：

- `figures/problem1.png`

---

## 5 问题2：固定电价下的日前计划

问题2对 2025 年 2 月 1 日至 12 月 31 日逐日求解。每天使用 0:00 发布的光伏预测形成日前储能计划，随后使用附件2中的真实光伏进行两阶段结算。

### 5.1 统计结果

| 指标 | 结果 |
|---|---:|
| 模拟天数 | 334 天 |
| 计划购电成本 | 12,347,800.16 元 |
| 紧急购电成本 | 2,424,945.24 元 |
| 总成本 | 14,772,745.40 元 |
| 紧急购电成本占比 | 16.41% |
| 日均总成本 | 44,229.78 元/天 |

结果文件为：

- `results/result2.xlsx`
- `results/problem2.json`

主图为：

- `figures/problem2.png`

由于日前计划只在每日开始时更新一次，预测误差会在后续时段累积，导致实际光伏不足时产生较多紧急购电。

---

## 6 问题3：波动电价下的滚动 MPC

问题3选取 2025 年 6 月 21 日进行日内滚动优化。MPC 在每天的 0:00、6:00、12:00 和 18:00 更新一次预测，每次优化剩余时段，实际只执行未来 6 小时的控制量。

### 6.1 统计结果

| 指标 | 结果 |
|---|---:|
| 目标日期 | 2025-06-21 |
| 预测更新时刻 | 0:00、6:00、12:00、18:00 |
| 预测时域 | 24 小时 |
| 计划购电成本 | 17,121.54 元 |
| 紧急购电成本 | 149.52 元 |
| 总成本 | 17,271.06 元 |
| 紧急购电量 | 54.79 kWh |
| 充电量 | 19,076.08 kWh |
| 放电量 | 15,451.62 kWh |
| SOC始末值 | 6,000.00 → 6,000.00 kWh |

结果文件为：

- `results/result3.xlsx`
- `results/problem3.json`

主图为：

- `figures/problem3.png`

在该日，MPC通过多次吸收最新预测信息，将紧急购电量控制在较低水平。

---

## 7 问题4：全年滚动 MPC

问题4将问题3的滚动策略扩展到 2025 年 2 月 1 日至 12 月 31 日。全年每天在 0:00、6:00、12:00 和 18:00 进行滚动优化，使用附件4中的波动电价和附件2中的真实负载、真实光伏进行结算。

### 7.1 统计结果

| 指标 | 结果 |
|---|---:|
| 模拟天数 | 334 天 |
| 计划购电成本 | 13,370,072.77 元 |
| 紧急购电成本 | 1,053,943.59 元 |
| 总成本 | 14,424,016.36 元 |
| 紧急购电成本占比 | 7.31% |
| 日均总成本 | 43,185.68 元/天 |

结果文件为：

- `results/result4.xlsx`
- `results/problem4.json`

主图为：

- `figures/problem4.png`

---

## 8 方案比较与结论

### 8.1 主要结果对比

| 方案 | 场景 | 总成本 | 紧急成本占比 |
|---|---|---:|---:|
| 问题1 | 单日确定型优化 | 35,126.95 元 | 0% |
| 问题2 | 固定电价、日前计划 | 14,772,745.40 元 | 16.41% |
| 问题3 | 波动电价、夏至日MPC | 17,271.06 元 | 0.87% |
| 问题4 | 波动电价、全年MPC | 14,424,016.36 元 | 7.31% |

### 8.2 MPC相对日前计划的改进

问题4相较问题2：

\[
14,772,745.40-14,424,016.36
=348,729.04\ {\rm 元}.
\]

相对节省率为
\[
\frac{348,729.04}{14,772,745.40}
\times100\%
=2.36\%.
\]

紧急购电成本由 2,424,945.24 元降至 1,053,943.59 元，减少
\[
1,371,001.65\ {\rm 元}.
\]

这说明滚动更新预测能够有效降低光伏预测偏差带来的实时补购电成本。

### 8.3 管理含义

1. **提高预测更新频率有助于降低风险。**  
   每日一次的日前计划对预测误差较敏感，多次 MPC 更新可以及时修正储能动作。

2. **储能应同时利用价格和预测信息。**  
   在波动电价下，储能不仅用于弥补光伏误差，也可以将购电行为转移到较低价格时段。

3. **紧急购电是衡量策略鲁棒性的关键指标。**  
   单纯比较计划购电成本可能掩盖预测失误造成的高价实时购电，必须将两阶段结算成本一并统计。

4. **首末 SOC 约束保证日间比较公平。**  
   所有日调度均从 6,000 kWh 开始并回到 6,000 kWh，避免通过消耗或囤积储能人为降低单日成本。

---

## 9 工程实现与复现

### 9.1 代码结构

```text
src/
├── config.py
├── microgrid.py
├── problem1.py
├── problem2.py
├── problem3.py
└── problem4.py
```

其中：

- `config.py`：统一配置、数据路径和储能参数；
- `microgrid.py`：共享 LP 求解器和数据读取函数；
- `problem1.py`：单日确定型优化；
- `problem2.py`：全年日前计划与两阶段结算；
- `problem3.py`：夏至日滚动 MPC；
- `problem4.py`：全年滚动 MPC。

### 9.2 运行命令

从工程根目录执行：

```bash
python src/microgrid.py
python src/problem1.py
python src/problem2.py
python src/problem3.py
python src/problem4.py
```

运行结果分别写入工程根目录的 `results/`，图像写入 `figures/`。代码使用 `__file__` 定位工程根目录，不依赖当前工作目录。

### 9.3 可复现性与验证

工程统一使用配置中的随机种子 42。虽然当前 LP 求解本身是确定性的，但固定随机种子便于后续扩展情景模拟和敏感性分析。

共享引擎自检结果：

- 问题1全天成本：35,126.95 元；
- 末态 SOC：6,000.00 kWh；
- 最大供电缺口：\(1.14\times10^{-13}\) kWh；
- 附件2负载和光伏矩阵形状：365×144；
- 附件4波动电价矩阵形状：365×144；
- 附件3预测记录数：1,460 条。

---

## 10 局限性与后续改进

当前模型仍有以下可改进之处：

1. 未显式加入充放电互斥二进制变量，因此在线性模型中可能出现同一时段同时充电和放电的退化解；
2. 未加入储能衰减、循环寿命和功率爬坡约束；
3. 紧急购电仅作为缺口结算项，尚未显式建模实时调度的二次优化；
4. MPC使用确定性预测，尚未扩展到鲁棒优化或随机规划；
5. 预测发布时刻和控制时域还可以通过敏感性分析进一步优化。

后续可引入混合整数线性规划约束储能充放电互斥，并结合预测误差分布建立场景树或鲁棒 MPC，从而进一步提高模型的工程适用性。

---

## 结论

本工程已完成从数据读取、共享线性规划建模、单日验证、全年日前计划到全年滚动 MPC 的完整实现。所有核心脚本均已运行通过，结果文件和主图均已生成。

在当前参数和结算规则下，全年滚动 MPC 方案取得最低的总成本：

\[
\boxed{14,424,016.36\ {\rm 元}}
\]

相较全年日前计划方案节省：

\[
\boxed{348,729.04\ {\rm 元},\quad 2.36\%}.
\]

同时，紧急购电成本占比由 16.41% 降至 7.31%，表明滚动更新预测对于降低微网储能调度风险具有明显效果。
